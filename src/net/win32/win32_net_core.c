internal Net_Socket
net_open(U16 port, Net_Protocol protocol)
{
	Net_Socket result = { 0 };
	return(result);
}

internal U64
net_send(Net_Socket socket, Str8 data)
{
	U64 result = 0;
	return(result);
}

internal U64
net_recieve(Net_Socket socket, U8 *buffer, U64 buffer_size)
{
	U64 result = 0;
	return(result);
}

internal Void
net_close(Net_Socket socket)
{
}